{
  "name": "product-service",
  "version": "1.0.0",
  "description": "Product service (API based)",
  "main": "handler.js",
  "scripts": {
    "test": "jest ",
    "deploy": "serverless product-service deploy --no-config-change --no-policy-change --no-cors-change",
    "start": "serverless offline -P 8000 -o 0.0.0.0",
    "watch": "SLS_DEBUG=* serverless offline -P 8000 -o 0.0.0.0",
    "test:watch": "jest --watch  --experimental-vm-modules"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel": "^6.23.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1"
  },
  "devDependencies": {
    "jest": "^26.6.2",
    "serverless-offline": "^6.8.0",
    "serverless-webpack": "^5.3.5",
    "webpack": "^4.44.1",
    "webpack-node-externals": "^2.5.0"
  }
}
